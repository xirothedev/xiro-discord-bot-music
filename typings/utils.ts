export enum Category {
    music = "music",
    info = "info",
    dev = "dev",
    filters = "filters",
    playlist = "playlist",
}

export enum Mode {
    production = "production",
    development = "development",
    test = "test",
}